import React from 'react';
import ReactDOM from 'react-dom/client';
import KitInterpretarParaEvoluir from './index';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <KitInterpretarParaEvoluir />
  </React.StrictMode>
);
